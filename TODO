TODO :
parse headers
create unique tmpfile
saving budy in tmpfile
serve files on GET
upload file on POST
cgi

add timeout to epoll and timestamp to client

if config location end with slash match only with url that end with / change getLocation 

